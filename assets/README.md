# Organization brand assets

Canonical copies of the **antora-supplemental** mark for org-wide use (profiles, docs chrome, welcome site).

| File | Description |
|------|-------------|
| `logo.svg` | Transparent brand mark (docs header, README embeds) |
| `logo-avatar.svg` | 256 viewBox with soft green squircle — source for raster avatar |
| `logo-256.png` | 256×256 PNG for GitHub **Organization settings → Profile → Avatar** |

Keep these in sync with `.github-private/assets/` and the welcome-site / docs copies when the mark changes.
